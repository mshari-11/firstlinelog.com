# إعداد النطاق والربط مع Cloudflare
# دليل شامل لربط www.fll.sa

## 📋 نظرة عامة

هذا الدليل يشرح بالتفصيل كيفية ربط نطاق www.fll.sa مع التطبيق عبر Cloudflare Enterprise.

---

## 🌐 المعلومات الأساسية

```
النطاق الرئيسي: fll.sa
النطاق الفرعي: www.fll.sa
رابط Skywork: https://firstlinelog.skywork.website/
خطة Cloudflare: Enterprise ($200/شهر)
```

**لماذا Cloudflare Enterprise؟**
- ✅ سرعة فائقة في جميع أنحاء العالم
- ✅ حماية DDoS قوية
- ✅ Web Application Firewall (WAF)
- ✅ دعم فني 24/7
- ✅ SSL مجاني
- ✅ ضمان 100% Uptime

---

## 🚀 خطوات الإعداد الكاملة

### الخطوة 1️⃣: إنشاء حساب وإضافة النطاق

#### 1.1 التسجيل في Cloudflare

1. اذهب إلى: https://dash.cloudflare.com
2. اضغط "Sign Up" (تسجيل)
3. أدخل البريد الإلكتروني وكلمة المرور
4. فعّل الحساب عبر البريد

#### 1.2 إضافة النطاق

1. من لوحة التحكم، اضغط "Add a Site" (إضافة موقع)
2. أدخل: `fll.sa`
3. اختر الخطة: **Enterprise**
4. اضغط "Add Site" (إضافة الموقع)
5. انتظر فحص النطاق (قد يستغرق دقيقة)

#### 1.3 تحديث خوادم الأسماء (Nameservers)

**خطوات التحديث:**

1. سيعطيك Cloudflare nameservers مخصصة، مثل:
   ```
   alice.ns.cloudflare.com
   bob.ns.cloudflare.com
   ```

2. اذهب إلى مزود النطاق (مثل: SaudiNIC أو GoDaddy)
3. ابحث عن إعدادات DNS أو Nameservers
4. استبدل الـ nameservers القديمة بالجديدة من Cloudflare
5. احفظ التغييرات

**⏱️ مدة التفعيل:**
- قد يستغرق من 2 إلى 48 ساعة
- عادةً يكتمل في 2-4 ساعات
- ستصلك رسالة بريد إلكتروني عند الانتهاء

---

### الخطوة 2️⃣: إعداد سجلات DNS

بعد تفعيل النطاق، أضف السجلات التالية:

#### سجلات A (للربط المباشر بالسيرفر)

إذا كان لديك عنوان IP للسيرفر:

```
النوع: A
الاسم: @
المحتوى: [عنوان IP الخاص بالسيرفر]
حالة Proxy: مفعل (☁️ البرتقالية)
TTL: تلقائي

النوع: A
الاسم: www
المحتوى: [عنوان IP الخاص بالسيرفر]
حالة Proxy: مفعل (☁️ البرتقالية)
TTL: تلقائي
```

#### سجلات CNAME (للربط مع خدمات مثل Vercel أو Netlify)

إذا كنت تستخدم Vercel، Netlify، أو Skywork:

```
النوع: CNAME
الاسم: www
المحتوى: firstlinelog.skywork.website
حالة Proxy: مفعل (☁️ البرتقالية)
TTL: تلقائي
```

**للربط مع النطاق الرئيسي (بدون www):**

```
النوع: CNAME
الاسم: @
المحتوى: firstlinelog.skywork.website
حالة Proxy: مفعل (☁️ البرتقالية)
TTL: تلقائي
```

**⚠️ ملاحظة مهمة:** بعض مزودي الخدمة لا يسمحون بـ CNAME للنطاق الرئيسي (@). في هذه الحالة، استخدم سجل A أو ALIAS.

#### سجلات TXT (للتحقق والأمان)

```
النوع: TXT
الاسم: @
المحتوى: v=spf1 include:_spf.google.com ~all
TTL: تلقائي
```

هذا لإعداد SPF لحماية البريد الإلكتروني.

---

### الخطوة 3️⃣: إعداد SSL/TLS (الشهادة الأمنية)

#### 3.1 اختيار وضع التشفير

1. من لوحة Cloudflare، اذهب إلى: **SSL/TLS**
2. في قسم **Overview**
3. اختر وضع التشفير: **Full (strict)** ✅

**أوضاع التشفير المتاحة:**
- ❌ **Off:** بدون تشفير (غير آمن)
- ❌ **Flexible:** تشفير بين المتصفح و Cloudflare فقط
- ⚠️ **Full:** تشفير كامل لكن بدون التحقق من الشهادة
- ✅ **Full (strict):** تشفير كامل مع التحقق من الشهادة (الأفضل)

#### 3.2 إنشاء شهادة Origin

لحماية الاتصال بين Cloudflare والسيرفر:

1. اذهب إلى: **SSL/TLS → Origin Server**
2. اضغط "Create Certificate"
3. اختر الإعدادات:
   - **نوع المفتاح الخاص:** RSA (2048)
   - **النطاقات:**
     - `fll.sa`
     - `*.fll.sa` (لجميع النطاقات الفرعية)
   - **مدة الصلاحية:** 15 سنة
4. اضغط "Create"

**ستحصل على:**
- Origin Certificate (الشهادة)
- Private Key (المفتاح الخاص)

**⚠️ مهم جداً:**
- احفظ الشهادة والمفتاح في مكان آمن
- لن تستطيع رؤية المفتاح مرة أخرى
- ستحتاجها لتثبيتها على السيرفر

#### 3.3 تثبيت الشهادة على السيرفر

##### إذا كنت تستخدم Nginx:

```bash
# إنشاء مجلد للشهادات
sudo mkdir -p /etc/nginx/ssl

# حفظ الشهادة
sudo nano /etc/nginx/ssl/fll.sa.crt
# الصق Origin Certificate هنا

# حفظ المفتاح الخاص
sudo nano /etc/nginx/ssl/fll.sa.key
# الصق Private Key هنا

# حماية المفتاح الخاص
sudo chmod 600 /etc/nginx/ssl/fll.sa.key
```

**تحديث إعدادات Nginx:**

```nginx
server {
    listen 443 ssl http2;
    server_name fll.sa www.fll.sa;
    
    # شهادة SSL
    ssl_certificate /etc/nginx/ssl/fll.sa.crt;
    ssl_certificate_key /etc/nginx/ssl/fll.sa.key;
    
    # إعدادات SSL الآمنة
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers HIGH:!aNULL:!MD5;
    ssl_prefer_server_ciphers on;
    
    # باقي الإعدادات...
    root /var/www/fll.sa;
    index index.html;
    
    location / {
        try_files $uri $uri/ =404;
    }
}

# إعادة التوجيه من HTTP إلى HTTPS
server {
    listen 80;
    server_name fll.sa www.fll.sa;
    return 301 https://$server_name$request_uri;
}
```

**اختبار وإعادة تشغيل Nginx:**

```bash
# اختبار الإعدادات
sudo nginx -t

# إذا كان كل شيء صحيح، أعد التشغيل
sudo systemctl reload nginx
```

##### إذا كنت تستخدم Apache:

```bash
# حفظ الملفات
sudo nano /etc/apache2/ssl/fll.sa.crt
sudo nano /etc/apache2/ssl/fll.sa.key
sudo chmod 600 /etc/apache2/ssl/fll.sa.key
```

**تحديث VirtualHost:**

```apache
<VirtualHost *:443>
    ServerName fll.sa
    ServerAlias www.fll.sa
    
    SSLEngine on
    SSLCertificateFile /etc/apache2/ssl/fll.sa.crt
    SSLCertificateKeyFile /etc/apache2/ssl/fll.sa.key
    
    DocumentRoot /var/www/fll.sa
    
    <Directory /var/www/fll.sa>
        Options Indexes FollowSymLinks
        AllowOverride All
        Require all granted
    </Directory>
</VirtualHost>
```

```bash
# إعادة تشغيل Apache
sudo systemctl reload apache2
```

#### 3.4 تفعيل HSTS (HTTP Strict Transport Security)

لإجبار جميع المتصفحات على استخدام HTTPS:

1. اذهب إلى: **SSL/TLS → Edge Certificates**
2. قم بتفعيل:
   - ✅ **Always Use HTTPS**
   - ✅ **HTTP Strict Transport Security (HSTS)**
     - **Max Age Header:** 12 شهر
     - ✅ **Include subdomains**
     - ✅ **Preload**
     - ✅ **No-Sniff Header**
3. قم بتفعيل:
   - ✅ **Minimum TLS Version:** TLS 1.2
   - ✅ **Opportunistic Encryption**
   - ✅ **TLS 1.3**

**ما فائدة HSTS؟**
- ✅ يمنع هجمات Man-in-the-Middle
- ✅ يحسن الأمان
- ✅ يحسن SEO

---

### الخطوة 4️⃣: إعداد قواعد الصفحات (Page Rules)

قواعد الصفحات تسمح لك بالتحكم في سلوك Cloudflare لعناوين URL محددة.

#### القاعدة 1: فرض HTTPS

```
الرابط: http://*fll.sa/*
الإعداد: Always Use HTTPS
```

**الفائدة:** تحويل أي طلب HTTP تلقائياً إلى HTTPS

#### القاعدة 2: إعادة التوجيه من www إلى بدون www (أو العكس)

**خيار أ: من www إلى بدون www**

```
الرابط: www.fll.sa/*
الإعداد: Forwarding URL
النوع: 301 - Permanent Redirect
الوجهة: https://fll.sa/$1
```

**خيار ب: من بدون www إلى www**

```
الرابط: fll.sa/*
الإعداد: Forwarding URL
النوع: 301 - Permanent Redirect
الوجهة: https://www.fll.sa/$1
```

⚠️ **اختر واحد فقط!** استخدم إما www أو بدونها، ليس كليهما.

#### القاعدة 3: التخزين المؤقت للملفات الثابتة

```
الرابط: *.fll.sa/static/*
الإعدادات:
├─ Cache Level: Cache Everything
├─ Edge Cache TTL: 1 شهر
└─ Browser Cache TTL: 1 شهر
```

**الفائدة:** تسريع تحميل الصور والملفات الثابتة

#### القاعدة 4: تعطيل التخزين المؤقت لـ API

```
الرابط: *.fll.sa/api/*
الإعداد: Cache Level - Bypass
```

**الفائدة:** ضمان الحصول على بيانات محدثة من API

---

### الخطوة 5️⃣: تحسين الأداء

#### 5.1 تفعيل Auto Minify

لتقليل حجم الملفات:

1. اذهب إلى: **Speed → Optimization**
2. فعّل **Auto Minify:**
   - ✅ JavaScript
   - ✅ CSS
   - ✅ HTML

**الفائدة:** تحميل أسرع بنسبة 10-20%

#### 5.2 تفعيل ضغط Brotli

1. في نفس القسم، فعّل:
   - ✅ **Brotli**

**الفائدة:** ضغط أفضل من Gzip بنسبة 15-20%

#### 5.3 إعدادات التخزين المؤقت

1. اذهب إلى: **Caching → Configuration**
2. اضبط:
   - **Browser Cache TTL:** 4 ساعات
   - ✅ **Crawler Hints** (مفعل)

#### 5.4 تفعيل HTTP/2 و HTTP/3

1. اذهب إلى: **Network**
2. فعّل:
   - ✅ **HTTP/2**
   - ✅ **HTTP/3 (with QUIC)**
   - ✅ **0-RTT Connection Resumption**
   - ✅ **WebSockets**

**الفائدة:** سرعة أفضل وأمان أعلى

---

### الخطوة 6️⃣: الأمان المتقدم

#### 6.1 تفعيل Web Application Firewall (WAF)

1. اذهب إلى: **Security → WAF**
2. فعّل **Managed Rules:**
   - ✅ **Cloudflare Managed Ruleset**
   - ✅ **Cloudflare OWASP Core Ruleset**
   - ✅ **Cloudflare Exposed Credentials Check**

**الفائدة:**
- حماية من SQL Injection
- حماية من XSS attacks
- حماية من CSRF
- اكتشاف كلمات المرور المسربة

#### 6.2 تفعيل Bot Protection

1. اذهب إلى: **Security → Bots**
2. فعّل: **Super Bot Fight Mode** ✅

**الفائدة:**
- منع الروبوتات الضارة
- حماية من Credential Stuffing
- منع Web Scraping غير المصرح به

#### 6.3 إعدادات مستوى الأمان

1. اذهب إلى: **Security → Settings**
2. اضبط:
   - **Security Level:** Medium (أو High للحماية القصوى)
   - **Challenge Passage:** 30 دقيقة
   - ✅ **Privacy Pass Support**

#### 6.4 تحديد معدل الطلبات (Rate Limiting)

لمنع إساءة الاستخدام:

1. اذهب إلى: **Security → WAF → Rate limiting rules**
2. أنشئ قاعدة جديدة:

**مثال: حماية صفحة تسجيل الدخول**

```
الاسم: حماية تسجيل الدخول
الشرط: 
  - URI Path يساوي /login
  - OR URI Path يساوي /api/login
الإجراء:
  - عندما تصل الطلبات إلى: 5 طلبات
  - خلال: 1 دقيقة
  - الإجراء: Block (حظر)
  - المدة: 10 دقائق
```

---

### الخطوة 7️⃣: الربط مع Vercel أو Netlify

إذا كنت تستضيف الموقع على Vercel أو Netlify:

#### للربط مع Vercel:

1. اذهب إلى لوحة Vercel
2. اختر المشروع
3. اذهب إلى **Settings → Domains**
4. أضف النطاقات:
   - `www.fll.sa`
   - `fll.sa`
5. Vercel سيعطيك سجل CNAME، مثل:
   ```
   cname.vercel-dns.com
   ```
6. أضف هذا السجل في Cloudflare DNS (كما في الخطوة 2)

#### للربط مع Netlify:

1. اذهب إلى لوحة Netlify
2. اختر الموقع
3. اذهب إلى **Domain settings**
4. اضغط "Add custom domain"
5. أدخل: `www.fll.sa`
6. أضف سجل CNAME في Cloudflare يشير إلى Netlify

#### تحديث متغيرات البيئة:

```env
# في ملف .env
VITE_APP_URL=https://www.fll.sa
VITE_APP_DOMAIN=fll.sa
VITE_SKYWORK_URL=https://firstlinelog.skywork.website/
```

---

### الخطوة 8️⃣: الاختبار والتحقق

#### 8.1 اختبار DNS

```bash
# فحص سجلات DNS
nslookup www.fll.sa

# أو باستخدام dig
dig www.fll.sa

# التحقق من انتشار DNS عالمياً
# اذهب إلى: https://www.whatsmydns.net/
# أدخل: www.fll.sa
```

**النتيجة المتوقعة:**
```
www.fll.sa → CNAME → firstlinelog.skywork.website
أو
www.fll.sa → A → [عنوان IP]
```

#### 8.2 اختبار SSL

```bash
# فحص شهادة SSL
openssl s_client -connect www.fll.sa:443 -servername www.fll.sa

# أو استخدم أداة أونلاين:
# https://www.ssllabs.com/ssltest/analyze.html?d=www.fll.sa
```

**النتيجة المتوقعة:**
- ✅ تقييم A أو A+
- ✅ TLS 1.2 أو 1.3
- ✅ الشهادة صالحة
- ✅ بدون أخطاء

#### 8.3 اختبار إعادة التوجيه من HTTP إلى HTTPS

```bash
curl -I http://www.fll.sa
```

**النتيجة المتوقعة:**
```
HTTP/1.1 301 Moved Permanently
Location: https://www.fll.sa/
```

#### 8.4 اختبار الموقع

```bash
# اختبار الصفحة الرئيسية
curl -I https://www.fll.sa

# النتيجة المتوقعة:
# HTTP/2 200
# server: cloudflare
# cf-ray: [ray-id]
```

#### 8.5 فحص ميزات Cloudflare

```bash
# التحقق من تفعيل Cloudflare
curl -I https://www.fll.sa | grep -i "cf-ray"

# التحقق من التخزين المؤقت
curl -I https://www.fll.sa | grep -i "cf-cache-status"

# التحقق من ضغط Brotli
curl -H "Accept-Encoding: br" -I https://www.fll.sa | grep -i "content-encoding"
```

#### 8.6 اختبار السرعة

استخدم أدوات أونلاين:

1. **GTmetrix:** https://gtmetrix.com
   - أدخل: www.fll.sa
   - التقييم المتوقع: A أو B

2. **Google PageSpeed Insights:** https://pagespeed.web.dev/
   - أدخل: www.fll.sa
   - التقييم المتوقع: 80+

3. **WebPageTest:** https://www.webpagetest.org/
   - أدخل: www.fll.sa
   - اختبر من مواقع متعددة

---

## 📊 المراقبة والإحصائيات

### إحصائيات Cloudflare

1. اذهب إلى: **Analytics → Traffic**
2. راقب:
   - عدد الطلبات
   - Bandwidth المستخدم
   - الزوار الفريدين
   - التهديدات المحظورة
   - معدل التخزين المؤقت (Cache Hit Ratio)

**المؤشرات المهمة:**
- ✅ Cache Hit Ratio فوق 80%
- ✅ SSL Usage: 100%
- ✅ Threats Mitigated: يعرض الهجمات المحظورة

### إعداد التنبيهات

1. اذهب إلى: **Notifications**
2. أضف تنبيهات لـ:
   - ✅ انتهاء صلاحية SSL
   - ✅ زيادة غير عادية في الزيارات
   - ✅ أخطاء في السيرفر (5xx)
   - ✅ هجمات DDoS
   - ✅ أخطاء في التحقق من الشهادة

---

## 🛠️ حل المشاكل الشائعة

### مشكلة 1: الموقع لا يعمل

**الأعراض:**
- خطأ "This site can't be reached"
- ERR_NAME_NOT_RESOLVED

**الحلول:**

```bash
# 1. تحقق من DNS
nslookup www.fll.sa

# 2. تحقق من Cloudflare
curl -I https://www.fll.sa

# 3. تحقق من السيرفر الأصلي
curl -I http://[origin-ip]

# 4. راجع سجلات Cloudflare
# في لوحة التحكم: Analytics → Security
```

**الخطوات:**
1. تأكد أن Nameservers محدثة
2. تحقق من سجلات DNS في Cloudflare
3. تأكد أن السيرفر يعمل
4. راجع إعدادات Proxy (البرتقالية)

---

### مشكلة 2: خطأ في شهادة SSL

**الأعراض:**
- "Your connection is not private"
- NET::ERR_CERT_AUTHORITY_INVALID

**الحلول:**

```bash
# 1. تحقق من الشهادة
openssl s_client -connect www.fll.sa:443

# 2. تحقق من وضع SSL في Cloudflare
# يجب أن يكون: Full (strict)

# 3. تحقق من تثبيت Origin Certificate على السيرفر
```

**الخطوات:**
1. تأكد من اختيار **Full (strict)** في إعدادات SSL
2. تحقق من تثبيت Origin Certificate على السيرفر
3. تأكد من صلاحية الشهادة
4. أعد تشغيل الويب سيرفر (Nginx/Apache)

---

### مشكلة 3: الموقع بطيء

**الأعراض:**
- وقت تحميل طويل
- TTFB (Time To First Byte) عالي

**الحلول:**

```bash
# 1. فحص حالة Cache
curl -I https://www.fll.sa | grep -i cf-cache-status

# 2. فحص CDN
curl -I https://www.fll.sa | grep -i cf-ray
```

**الخطوات:**
1. تفعيل Auto Minify
2. تفعيل Brotli Compression
3. إعداد Page Rules للتخزين المؤقت
4. تفعيل HTTP/3
5. (اختياري) تفعيل Argo Smart Routing

---

### مشكلة 4: أخطاء 520 أو 521 أو 522

**الأعراض:**
- Error 520: Web server is returning an unknown error
- Error 521: Web server is down
- Error 522: Connection timed out

**الحلول:**

```bash
# 1. تحقق من حالة السيرفر
curl -I http://[origin-ip]

# 2. تحقق من جدار الحماية
# تأكد أن IP عناوين Cloudflare مسموحة

# 3. راجع سجلات السيرفر
sudo tail -f /var/log/nginx/error.log
# أو
sudo tail -f /var/log/apache2/error.log
```

**الخطوات:**
1. تأكد أن السيرفر يعمل
2. اسمح بـ IP عناوين Cloudflare في الجدار الناري
3. تحقق من إعدادات SSL على السيرفر
4. راجع سجلات الأخطاء

---

## 💰 التكاليف

### Cloudflare Enterprise: $200/شهر

**يشمل:**
- ✅ حماية DDoS غير محدودة
- ✅ Web Application Firewall (WAF) متقدم
- ✅ ضمان 100% Uptime (SLA)
- ✅ شهادات SSL مخصصة
- ✅ دعم فني ذو أولوية 24/7
- ✅ إحصائيات متقدمة
- ✅ تحسين الصور
- ✅ Load Balancing

### الإضافات الاختيارية:

```
Argo Smart Routing: $5/شهر + $0.10/GB
├─ تحسين السرعة بنسبة 30%
└─ توجيه ذكي للطلبات

Load Balancing: $5/origin/شهر
├─ توزيع الحمل على عدة خوادم
└─ فشل تلقائي (Failover)

Rate Limiting: $5/شهر لكل 10,000 طلب
├─ حماية من إساءة الاستخدام
└─ تحديد معدل الطلبات
```

---

## ✅ قائمة التحقق النهائية

### الإعداد الأساسي:
- [ ] إنشاء حساب Cloudflare
- [ ] إضافة النطاق fll.sa
- [ ] تحديث Nameservers
- [ ] انتظار تفعيل النطاق (2-48 ساعة)

### سجلات DNS:
- [ ] إضافة سجل A أو CNAME للنطاق الرئيسي (@)
- [ ] إضافة سجل A أو CNAME للـ www
- [ ] إضافة سجل TXT لـ SPF (اختياري)
- [ ] تفعيل Proxy (البرتقالية) لجميع السجلات

### SSL/TLS:
- [ ] اختيار وضع Full (strict)
- [ ] إنشاء Origin Certificate
- [ ] تثبيت الشهادة على السيرفر
- [ ] تفعيل Always Use HTTPS
- [ ] تفعيل HSTS
- [ ] تفعيل Minimum TLS 1.2

### Page Rules:
- [ ] قاعدة Force HTTPS
- [ ] قاعدة إعادة التوجيه (www ↔ non-www)
- [ ] قاعدة التخزين المؤقت للملفات الثابتة

### التحسين:
- [ ] تفعيل Auto Minify (JS, CSS, HTML)
- [ ] تفعيل Brotli
- [ ] إعداد Browser Cache TTL
- [ ] تفعيل HTTP/2 و HTTP/3

### الأمان:
- [ ] تفعيل WAF Managed Rules
- [ ] تفعيل Bot Protection
- [ ] ضبط Security Level
- [ ] إعداد Rate Limiting للصفحات الحساسة

### الاختبار:
- [ ] اختبار DNS (nslookup)
- [ ] اختبار SSL (ssllabs.com)
- [ ] اختبار إعادة التوجيه HTTP→HTTPS
- [ ] اختبار الموقع (curl)
- [ ] اختبار السرعة (GTmetrix)
- [ ] اختبار من متصفحات مختلفة

### المراقبة:
- [ ] إعداد التنبيهات لانتهاء SSL
- [ ] إعداد تنبيهات للأخطاء
- [ ] إعداد تنبيهات للهجمات
- [ ] مراجعة الإحصائيات بانتظام

---

## 📞 الدعم

### دعم Cloudflare:

- **البريد الإلكتروني:** support@cloudflare.com
- **الهاتف:** +1 (888) 993-5273 (Enterprise)
- **البوابة:** https://dash.cloudflare.com/support
- **حالة الخدمة:** https://www.cloudflarestatus.com

### الموارد:

- **التوثيق:** https://developers.cloudflare.com
- **المجتمع:** https://community.cloudflare.com
- **مدونة:** https://blog.cloudflare.com

### للدعم التقني المحلي:

- **البريد:** tech@fll.sa
- **الموقع:** www.fll.sa

---

## 🎯 الخلاصة

باتباع هذا الدليل، ستحصل على:

✅ **نطاق محمي** بـ Cloudflare Enterprise  
✅ **SSL مفعّل** مع تقييم A+  
✅ **سرعة فائقة** عبر CDN عالمي  
✅ **حماية كاملة** من DDoS والهجمات  
✅ **مراقبة مستمرة** 24/7  

**التكلفة:** $200/شهر (Cloudflare Enterprise)  
**وقت الإعداد:** 2-4 ساعات (بالإضافة لوقت انتشار DNS)  
**مستوى الصعوبة:** متوسط  

**الحالة:** ✅ جاهز للتطبيق

---

**آخر تحديث:** فبراير 2026  
**الإصدار:** 1.0  
**اللغة:** العربية  
**المؤلف:** فريق البنية التحتية - فيرست لاين لوجستيكس
