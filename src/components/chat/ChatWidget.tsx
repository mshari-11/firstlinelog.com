/**
 * ChatWidget stub — placeholder for future chat integration.
 * Renders nothing for now; preserves the import contract in Layout.tsx.
 */
interface ChatWidgetProps {
  userRole: string;
  userId: string;
  userName: string;
}

export default function ChatWidget(_props: ChatWidgetProps) {
  return null;
}
